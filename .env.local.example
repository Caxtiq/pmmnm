# .env.local.example
# Hướng dẫn:
# 1. Sao chép file này đổi tên thành .env.local: cp .env.local.example .env.local
# 2. Điền các giá trị thực tế (Secrets) vào file .env.local (File này KHÔNG được commit lên git)

# ==============================================================================
# 1. FRONTEND CONFIGURATION (VietMap)
# ==============================================================================
# API Key của VietMap GL JS. Bắt buộc để hiển thị bản đồ nền.
# Lấy key tại: https://console.vietmap.vn/
NEXT_PUBLIC_VIETMAP_API_KEY=your_vietmap_api_key_here

# ==============================================================================
# 2. DATABASE CONFIGURATION
# ==============================================================================
# Chuỗi kết nối MongoDB (MongoDB Atlas hoặc Local).
# Ví dụ: mongodb+srv://user:pass@cluster.mongodb.net/dbname
MONGODB_URI=mongodb://localhost:27017/pmmnm_db

# ==============================================================================
# 3. EXTERNAL SERVICES (Weather)
# ==============================================================================
# API Key lấy dữ liệu thời tiết (OpenWeatherMap hoặc tương đương)
# Dùng để hiển thị lớp phủ thời tiết trên bản đồ.
WEATHER_API_KEY=your_openweather_api_key_here

# ==============================================================================
# 4. STORAGE CONFIGURATION (Google Cloud Storage)
# ==============================================================================
# Cấu hình lưu trữ hình ảnh phát hiện từ AI (Flood/Traffic detection images).
# Cần tạo Service Account trên Google Cloud Console.

GCS_BUCKET_NAME=your_storage_bucket_name
GCS_PROJECT_ID=your_gcp_project_id
GCS_CLIENT_EMAIL=service-account@your-project.iam.gserviceaccount.com

# Lưu ý: Private Key chứa các ký tự xuống dòng (\n), BẮT BUỘC để trong dấu ngoặc kép ("")
GCS_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIEvQIBADANBgkqhkiG9w0BAQEFAASCBKcwggSjAgEAAoIBAQC...\n-----END PRIVATE KEY-----\n"
